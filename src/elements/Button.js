import React from "react";
import styled from "styled-components";

const Button = (props) => {
  // const { } = props;
  return null;
};

//-- defaultProps --
Button.defaultProps = {};

export default Button;
