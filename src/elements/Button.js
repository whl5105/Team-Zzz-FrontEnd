import React from "react";
import styled from "styled-components";

const Button = (props) => {
  // const { } = props;
  // 버튼
  return null;
};

//-- defaultProps --
Button.defaultProps = {};

export default Button;
