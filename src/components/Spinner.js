import React from "react";
import styled from "styled-components";

const Spinner = (props) => {
  return (
    <>
      <div>
          <p>아이콘이에요</p>
      </div>
    </>
  );
};

export default Spinner;
